// Copy to secrets.h and fill in. secrets.h is gitignored.
#pragma once

#define WIFI_SSID     "your-network"
#define WIFI_PASSWORD "your-password"

// The Mac running bridge/quota_bridge.py. An IP is more reliable than mDNS
// on networks with client isolation or a flaky .local resolver.
#define BRIDGE_URL "http://192.168.1.117:8787/quota"
